"use client";

// Le compte est celui, unique, que le client cree (Clerk). Un outil n'expose pas
// sa propre suppression de compte ni ses reglages de compte: cette section a ete
// retiree. Elle ne rend rien.
export default function DeleteAccountSection() {
  return null;
}
